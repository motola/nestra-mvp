name: Deploy — Backend

on:
  push:
    branches: [develop]
    paths:
      - "backend/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-backend
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Fly.io
    runs-on: ubuntu-latest
    # Require CI to be green before deploying. In GitHub, wire this up via
    # a branch protection rule requiring the CI workflow to pass on main.
    environment: dev
    steps:
      - uses: actions/checkout@v4

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy
        working-directory: backend
        run: flyctl deploy --remote-only --release-command-timeout 600
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_TOKEN_BACKEND }}
